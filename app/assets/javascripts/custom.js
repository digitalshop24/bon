$(document).ready(function() {
	$('.eventCarousel').slick({
		arrows: false,
		autoplay: true,
		dots: true
	});

    $('.postCarousel').slick({
        arrows: false,
        autoplay: true,
        dots: true
    });

});